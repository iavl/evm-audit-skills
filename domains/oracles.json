{
  "id": "evm-audit-oracles",
  "name": "Oracle and Pricing Security",
  "checklist_title": "Oracle & Pricing Security Checklist",
  "description": "Security review for price feeds, TWAPs, VRF, and external data inputs.",
  "surface_features": [
    "uses-oracle"
  ],
  "related_domains": [
    "evm-audit-chain-specific",
    "evm-audit-precision-math"
  ],
  "always_screen": false,
  "required_context": [
    {
      "key": "feed_contracts",
      "required": true,
      "description": "feed contracts and data sources"
    },
    {
      "key": "feed_decimals",
      "required": true,
      "description": "feed and protocol decimal scaling"
    },
    {
      "key": "heartbeat",
      "required": true,
      "description": "heartbeat and staleness handling"
    },
    {
      "key": "bounds",
      "required": true,
      "description": "answer bounds, deviation, and circuit breakers"
    },
    {
      "key": "fallback_behavior",
      "required": true,
      "description": "fallback, sequencer, and failure behavior"
    }
  ],
  "review_requirements": [
    "verify freshness, scaling, sequencer, and manipulation resistance"
  ],
  "trusted_absence_policy": {"requires_complete_scope": true, "allowed_evidence": ["scope", "inheritance", "interface", "dependency", "deployment"]}
}

{
  "id": "evm-audit-defi-staking",
  "name": "Staking and Restaking Security",
  "checklist_title": "Staking & LSD Security Checklist",
  "description": "Security review for staking, liquid staking, restaking, rewards, and yield aggregation.",
  "surface_features": [
    "uses-staking"
  ],
  "related_domains": [
    "evm-audit-precision-math",
    "evm-audit-erc20"
  ],
  "always_screen": false,
  "required_context": [
    {
      "key": "reward_accounting",
      "required": true,
      "description": "reward accrual, distribution, and claim accounting"
    },
    {
      "key": "withdrawal_queue",
      "required": true,
      "description": "withdrawal, cooldown, and exit queue behavior"
    },
    {
      "key": "validator_model",
      "required": true,
      "description": "validator, delegation, and restaking model"
    },
    {
      "key": "slashing_assumptions",
      "required": true,
      "description": "slashing, loss, and operator assumptions"
    }
  ],
  "review_requirements": [
    "model share/reward accounting and delayed exits"
  ],
  "trusted_absence_policy": {"requires_complete_scope": true, "allowed_evidence": ["scope", "inheritance", "interface", "dependency", "deployment"]}
}

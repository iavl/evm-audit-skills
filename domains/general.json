{
  "id": "evm-audit-general",
  "name": "General Solidity/EVM Security",
  "checklist_title": "General Solidity/EVM Security Checklist",
  "description": "General Solidity and EVM security review for every smart contract.",
  "surface_features": [
    "uses-general"
  ],
  "related_domains": [
    "evm-audit-precision-math"
  ],
  "always_screen": true,
  "required_context": [
    {
      "key": "scope_inventory",
      "required": true,
      "description": "complete source, dependency, and deployment scope inventory"
    },
    {
      "key": "entry_points",
      "required": true,
      "description": "state-changing entry points and call paths"
    },
    {
      "key": "trust_boundaries",
      "required": true,
      "description": "privilege, user, oracle, and external trust boundaries"
    },
    {
      "key": "external_dependencies",
      "required": true,
      "description": "external contracts, libraries, and integration assumptions"
    }
  ],
  "review_requirements": [
    "trace reachable state changes and cross-domain interactions"
  ],
  "trusted_absence_policy": {"requires_complete_scope": true, "allowed_evidence": ["scope", "inheritance", "interface", "dependency", "deployment"]}
}

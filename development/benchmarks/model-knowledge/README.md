# Model Knowledge Benchmarks

The files under this directory are dated, model-specific benchmark snapshots.
They are preserved for comparison and evaluation only and are not runtime
skills or canonical checklist sources.

Runtime audits use `data/canonical-checks.json` and the generated domain
checklists. Do not route an audit by whether a particular model is described as
already knowing a pattern.

# Recon and Routing

Run Recon against the complete audit root. Its Feature Map v3 records the
scope digest and compilation coverage. Selector rejects a missing, incomplete,
or mismatched scope before filtering. Routing v4 applies environment, Domain,
then canonical feature gates; unknown values remain selected.

# Adding a Check

Edit `data/canonical-checks.json` with a stable `canonical_id`, owner Domain,
trigger, detection, false-positive gates, proof obligation, predicate, evidence
provenance, verification, and freshness. Keep semantic duplicates linked with
`related` and record dedup decisions. Run the generator and strict validator.
